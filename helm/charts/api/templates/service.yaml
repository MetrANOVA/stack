apiVersion: v1
kind: Service
metadata:
  name: {{ include "metranova-api.fullname" . }}
  labels:
    {{- include "metranova-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "metranova-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
