# OIDC client credentials for the Envoy proxy client.
# CHANGEME values are replaced by build.py during 'make docker'.
ENVOY_CLIENT_SECRET=CHANGEME

# HMAC secret for Envoy OAuth2 cookie signing.
# CHANGEME value is replaced by build.py during 'make docker'.
ENVOY_HMAC_SECRET=CHANGEME
