
stacks:
  - type: flow
    collectors:
      - type: nfacctd
        enabled: true
    pipeline:
      enabled: true

message_bus:
  type: kafka
  enabled: true

datastore:
  type: clickhouse
  enabled: true
